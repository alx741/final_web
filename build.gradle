apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src/app'
        }
    }
}

task doc(type:Exec) {
    workingDir 'doc/'
    commandLine 'make'
}

clean.doFirst {
    delete "${rootDir}/doc/generated"
}
