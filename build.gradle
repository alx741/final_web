apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src/app'
        }
    }
}

task doc(type:Exec) {
    workingDir 'doc/'
    commandLine 'make'
}

task clean_doc(type:Exec) {
    workingDir 'doc/'
    commandLine 'make', 'clean'
}

clean.finalizedBy(clean_doc)

build.finalizedBy(doc)
